% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clip_square_interactive.R
\name{clip_square_interactive}
\alias{clip_square_interactive}
\title{Interactive Square ROI Clipping with lidR}
\usage{
clip_square_interactive(
  las,
  width = 100,
  res = 1,
  subcircle = 0,
  return_type = c("las", "both", "polygon")
)
}
\arguments{
\item{las}{A character string (path to a LAS/LAZ) or a LAS object.}

\item{width}{Numeric. Side length of the square ROI (same units as CRS). Default: 100.}

\item{res}{Numeric. Resolution (in same units) for rasterizing the CHM. Default: 1.}

\item{subcircle}{Numeric. \code{p2r} subcircle for canopy rasterization. Default: 0.}

\item{return_type}{Character. What to return: "las" (default), "both", or "polygon".
\itemize{
\item "las": return the clipped LAS object (original behavior)
\item "polygon": return only the square ROI sf polygon (sfc POLYGON)
\item "both": return a named list with \code{list(las = <LAS>, roi = <sfc POLYGON>)}
}}
}
\value{
Depending on \code{return_type}:
\itemize{
\item "las": a LAS object containing only the points within the square ROI
\item "polygon": an \code{sf} \code{sfc} POLYGON of the selected ROI
\item "both": a named list with elements \code{las} and \code{roi}
}
}
\description{
Interactively clips a square ROI from a LAS dataset.
It first builds a canopy height model and displays it. Then the user
clicks the centre of the square. A square polygon is constructed and used
to clip the LAS dataset.
}
\examples{
\dontrun{
  # Example using the Megaplot dataset
  lasFile <- system.file("extdata", "Megaplot.laz", package = "lidR")

  las_clip <- clip_square_interactive(
    las       = lasFile,
    width     = 15,
    res       = 1,
    subcircle = 0.1,
    return_type = "las"
  )
}

}
